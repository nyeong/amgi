schema: amgi_v1
name: StableIdentity
global_tags: []
note_schema:
  id: "{{target}}"
  required_fields:
    - target
    - meaning
  optional_fields:
    - memo
cards:
  - name: Recall Meaning
    default: true
    front: "{{target}}"
    back: |
      {{meaning}}
      <div>{{memo}}</div>
