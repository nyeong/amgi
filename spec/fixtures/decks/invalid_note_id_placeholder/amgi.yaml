schema: amgi_v1
name: InvalidNoteIdPlaceholder
note_schema:
  id: "{{target}}-{{unknownField}}"
  required_fields:
    - target
    - meaning
  optional_fields: []
cards:
  - name: Recall Meaning
    default: true
    front: "{{target}}"
    back: "{{meaning}}"
