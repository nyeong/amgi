schema: amgi_v1
name: Broken_Deck
note_schema:
  id: "{{target}}"
  required_fields:
    - target
    - meaning
  optional_fields:
    - example
cards:
  - name: Card 1
    default: true
    front: "{{target}}"
    back: "{{meaning}}"
