schema: amgi_v1
name: Broken_Template
note_schema:
  required_fields:
    - target
  optional_fields: []
cards:
  - id: cardOne
    name: Card 1
    default: true
    front: "{{unknownField}}"
    back: "{{target}}"
