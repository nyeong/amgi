schema: amgi_v1
name: Broken_Template
note_schema:
  id: "{{target}}"
  required_fields:
    - target
  optional_fields: []
cards:
  - name: Card 1
    default: true
    front: "{{unknownField}}"
    back: "{{target}}"
