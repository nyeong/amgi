schema: amgi_v1
name: Broken_No_Required
note_schema:
  id: "{{target}}"
  required_fields: []
  optional_fields:
    - target
cards:
  - name: Card 1
    default: true
    front: "{{target}}"
    back: "{{FrontSide}}"
