schema: amgi_v1
name: Broken_No_Required
note_schema:
  required_fields: []
  optional_fields:
    - target
cards:
  - id: cardOne
    name: Card 1
    default: true
    front: "{{target}}"
    back: "{{FrontSide}}"
