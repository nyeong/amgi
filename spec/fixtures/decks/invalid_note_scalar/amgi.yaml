schema: amgi_v1
name: InvalidNoteScalar
note_schema:
  id: "{{target}}"
  required_fields:
    - target
    - meaning
  optional_fields: []
cards:
  - name: Recall Meaning
    default: true
    front: "{{target}}"
    back: "{{meaning}}"
